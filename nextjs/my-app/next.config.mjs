/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // distDir: "out",
  reactStrictMode: false,
  compress: false,
  eslint: false,
  webpack: true,
};

export default nextConfig;
